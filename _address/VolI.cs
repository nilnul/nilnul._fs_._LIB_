namespace nilnul.fs._address
{
	/// <summary>
	/// for each main partition, a vol (logical drive ) can be established. for extension partition, multiple volumes can be established.
	/// 
	/// </summary>
	public interface VolI : _vol_.NameI
	{
	}
}
