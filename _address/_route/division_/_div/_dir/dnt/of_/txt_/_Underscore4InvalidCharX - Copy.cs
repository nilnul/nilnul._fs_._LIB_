using System;
using System.Linq;

namespace nilnul.fs._address._dst.denote.construct_.txt_
{
	[Obsolete()]

	static public class _Underscore4InvalidCharX
	{
		static public string _Txt(string x)
		{
			return string.Join(""
				, x.Select(y => _denote.txt.be_._valid.cha_.Invalids.Lazy.contain(y) ? '_' : y)

		   );
		}
	}
}
