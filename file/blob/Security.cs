namespace nilnul.fs.file.blob
{
	/// <summary>

	/// </summary>
	public class Security
	{
	}
	/*
	Security descriptors belong to files, not to hard links, so if you specify a security attributes in a CreateHardLink call, this will change security settings for the file itself (and for all the file links).
	*/
}
